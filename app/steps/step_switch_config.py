import streamlit as st

def step_switch_config(st_module):
    """Step 1: Welcome screen"""
    st.header('Step 1 — Welcome')
    st.write('Welcome — click the top "Next" to begin wire mapping for your pickups.')
